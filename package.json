{
  "name": "cypressconfig",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local":"npx cypress run --headed --browser edge --config baseUrl=https://google.com",
    "stag":"npx cypress run --headed --browser edge --config baseUrl=https://facebook.com",
    "prod":"npx cypress run --headed --browser edge --config baseUrl=https://yahoo.com",
    "stag2":"npx cypress run --headed --browser edge --config-file staging-config.json",
    "prod2":"npx cypress run --headed --browser edge --config-file production-config.json",
    "local2":"npx cypress run --headed --browser edge --config-file local-config.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "9.7"
  }
}
